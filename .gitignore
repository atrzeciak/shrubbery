*.code-workspace
node_modules/
.wrangler/
.dev.vars
scripts/out/
scripts/__pycache__/
public/app/version.json
# Names your domain, database and bucket: keep it out of the repository.
wrangler.toml
.wrangler-real.toml.bak
.scrub-names
# Working notes. They were kept out of the published history by hand; ignoring them means a local
# copy can sit in the working tree without one `git add -A` putting it in front of the world.
# .superpowers/ is the sharpest case: its design notes name the family and the domain throughout,
# which is precisely what the published tree was scrubbed of.
TODO.md
docs/
.superpowers/
.claude/
